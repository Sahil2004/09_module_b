info:
  name: login user
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/09_module_b/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "s@s.com",
        "password": "Password@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
